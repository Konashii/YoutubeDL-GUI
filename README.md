# YoutubeDL GUI written in Python

This project is for those who want a bit more simpler process of downloading youtube videos using the popular github repository yt-dlp.

## How to use

### Make sure Python Libraries are installed
Run inside project directory:
```
pip install -r requirements.txt
```

### Simply run the code in your terminal of choice
```
python main.py
```